       <div>
            {{!-- <div class="notification loadingBox">
                Loading …
            </div>
            <div class="notification infoBox">
                <span>Info</span>
            </div> --}}
            <div class="notification errorBox">
                <span>{{message}}</span>
            </div>
        </div>

<section id="viewAddhotel">
            <h2>Add new hotel</h2>
            <form id="formAddhotel" action="/create" method="POST">
                <label for="hotel">Hotel name:</label>
                <input type="text" id="hotel" name="hotel" placeholder="Hotel name" value="{{oldInput.hotel}}">
                <label for="city">City:</label>
                <input type="text" id="city" name="city" placeholder="City" value="{{oldInput.city}}">
                <label for="free-rooms">Free rooms:</label>
                <input type="number" id="free-rooms" name="freeRooms" placeholder="Free rooms" value="{{oldInput.freeRooms}}">
                <label for="imgUrl">Image:</label>
                <input type="text" id="imgUrl" name="imgUrl" placeholder="" value="{{oldInput.imgUrl}}">

                <input type="submit" class="create" value="Add">
            </form>
        </section>

{{!-- <section class="notifications">
    <p class="notification-message">{{message}}</p>
</section>

<form class="text-center border border-light p-5" action="/create" method="POST">
    <h1>Create Course</h1>
    <div class="form-group">
        <label for="title">Title</label>
        <input type="text" class="form-control" placeholder="Course name" name="title" value="{{oldInput.title}}">
    </div>
    <div class="form-group">
        <label for="description">Description</label>
        <textarea class="form-control" placeholder="Description" name="description">{{oldInput.description}}</textarea>
    </div>
    <div class="form-group">
        <label for="imageUrl">Image url</label>
        <input type="text" class="form-control" placeholder="Image Url" name="imageUrl" value="{{oldInput.imageUrl}}">
    </div>
    <div class="form-group">
        <label for="duration">Duration</label>
        <input type="text" class="form-control" placeholder="4 weeks" name="duration" value="{{oldInput.duration}}">
    </div>
    <button type="submit" class="btn btn-primary">Create</button>
</form> --}}
