        <section id="viewCatalog" class="background-img">
              {{#each items}}
            <div class="added-hotels">
                <a href="/details/{{_id}}" class="added-hotel">
                    <img src="{{imgUrl}}" alt=""
                        class="picture-added-hotel">
                    <h3>{{hotel}}</h3>
                    <span>Free rooms: {{freeRooms}}</span>
                </a>
            </div>
              {{else}}
            <div class="guest">
                There are no Hotels found...
            </div>
            {{/each}}
        </section>

{{!-- примерен изглед --}}
{{!-- <div class="jumbotron jumbotron-fluid">
    <div class="container">
        <h1 class="display-4">Tutorials</h1>
        <p class="lead">Start Self-Learning Today: Be the best Developer in the company</p>
    </div>
</div>

<div>
    <h1 class="text-center">Top Courses</h1>
    <div class="container mt-3">
        <div class="row d-flex justify-content-center">

            <div class="card-deck">
                
            {{#each items}}
                <div class="card mb-4">
                    <img class="card-img-top"
                        src="{{imageUrl}}"
                        alt="Card image cap" width="400">
                    <div class="card-body">
                        <h4 class="card-title">{{title}}</h4>
                        <small class="float-right">Enrolled: {{countArr peopleWhoIncremented}}</small>
                        <small class="float-right">Enrolled: {{peopleWhoIncremented.length}}</small> ВТОРИ ВАРИАНТ
                    </div>
                </div>
            {{else}}
        <h4 class="text-center">No courses...</h4>
            {{/each}}

            </div>

        </div>

    </div>
</div> --}}
