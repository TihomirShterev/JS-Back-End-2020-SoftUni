<div class="shoes">
    {{#each items}}
<div class="shoe">
    <img src="{{ imageUrl }}">
    <h3>{{ name }}</h3>
    <a href="/details/{{ _id }}">Buy it for ${{ price }}</a>
</div>
    {{else}}
    <h1>No items to display. Be the first to create a new offer...</h1>
    {{/each}}
</div>